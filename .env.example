# Get a free token at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=

# REQUIRED — Render Postgres "Internal Database URL" (from your database's info page on Render)
# Without this, submitting or viewing cases/stats will fail with a database error.
DATABASE_URL=
